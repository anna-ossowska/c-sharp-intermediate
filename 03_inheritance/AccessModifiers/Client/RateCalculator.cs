using System;

namespace Client
{
    // internal keyword makes RateCalculator accessible only inside the Client assembly
    internal class RateCalculator
    {
        public int Calculate(Customer customer)
        {
            return 0;
        }
    }
}
